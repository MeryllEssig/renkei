{
  "name": "@test/mcp-pkg",
  "version": "1.0.0",
  "description": "A package with MCP servers",
  "author": "tester",
  "license": "MIT",
  "backends": ["claude"],
  "mcp": {
    "test-server": {
      "command": "node",
      "args": ["server.js"],
      "env": { "PORT": "3000" }
    }
  }
}
